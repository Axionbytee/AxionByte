import { useEffect } from "react";

export function useZoerIframe() {
  useEffect(() => {
    // Add any iframe-related logic here if needed
    // This is a placeholder hook for Zoer integration
  }, []);
}
